#include<string>
namespace consts
{
    const int WIDTH = 106;
    const int HEIGHT = 66;
    const std::string TITLE = "Last Man Standing";
    const int EDGE_POS = WIDTH/2+2;
    const int MESSAGE_NUMBER = 7;
    // CONST CHAR CONSTANTS
    const char CHAR_HLINE = 196;   // (HorzLine)
    const char CHAR_VLINE = 179;   // (VertLine)
    const char CHAR_NE = 191;   // (NE)
    const char CHAR_NW = 218;   // (NW)
    const char CHAR_SE = 217;   // (SE)
    const char CHAR_SW = 192;   // (SW)

    // Double lines walls:
    const char CHAR_DHLINE = 205;   // (DoubleHorzLine)
    const char CHAR_DVLINE = 186;   // (DoubleVertLine)
    const char CHAR_DNE = 187;   // (DoubleNE)
    const char CHAR_DNW = 201;   // (DoubleNW)
    const char CHAR_DSE = 188;   // (DoubleSE)
    const char CHAR_DSW = 200;   // (DoubleSW)

    // Single line vertical/horizontal junctions (T junctions):
    const char CHAR_TEEW = 180;   // (TeeWest)
    const char CHAR_TEEE = 195;   // (TeeEast)
    const char CHAR_TEEN = 193;   // (TeeNorth)
    const char CHAR_TEES = 194;   // (TeeSouth)

    // Double line vertical/horizontal junctions (T junctions):
    const char CHAR_DTEEW = 185;   // (DoubleTeeWest)
    const char CHAR_DTEEE = 204;   // (DoubleTeeEast)
    const char CHAR_DTEEN = 202;   // (DoubleTeeNorth)
    const char CHAR_DTEES = 203;   // (DoubleTeeSouth)

    // Block const characters:
    const char CHAR_BLOCK1=176;   // (Block1)
    const char CHAR_BLOCK2=177;   // (Block2)
    const char CHAR_BLOCK3=178;   // (Block3)

    // Cross-junction between two single line walls:
    const char CHAR_CROSS = 197;   // (Cross)

    // Arrows:
    const char CHAR_ARROW_N = 24;   // (ArrowNorth)
    const char CHAR_ARROW_S = 25;   // (ArrowSouth)
    const char CHAR_ARROW_E = 26;   // (ArrowEast)
    const char CHAR_ARROW_W = 27;   // (ArrowWest)

    // Arrows without tail:
    const char CHAR_ARROW2_N = 30;   // (ArrowNorthNoTail)
    const char CHAR_ARROW2_S = 31;   // (ArrowSouthNoTail)
    const char CHAR_ARROW2_E = 16;   // (ArrowEastNoTail)
    const char CHAR_ARROW2_W = 17;   // (ArrowWestNoTail)

    // Double arrows:
    const char CHAR_DARROW_H = 29;   // (DoubleArrowHorz)
    const char CHAR_ARROW_V = 18;   // (DoubleArrowVert)

    // GUI stuff:
    const char CHAR_CHECKBOX_UNSET = 224;
    const char CHAR_CHECKBOX_SET = 225;
    const char CHAR_RADIO_UNSET = 9;
    const char CHAR_RADIO_SET = 10;

    // Sub-pixel resolution kit:
    const char CHAR_SUBP_NW = 226;   // (SubpixelNorthWest)
    const char CHAR_SUBP_NE = 227;   // (SubpixelNorthEast)
    const char CHAR_SUBP_N = 228;   // (SubpixelNorth)
    const char CHAR_SUBP_SE = 229;   // (SubpixelSouthEast)
    const char CHAR_SUBP_DIAG = 230;   // (SubpixelDiagonal)
    const char CHAR_SUBP_E = 231;   // (SubpixelEast)
    const char CHAR_SUBP_SW = 232;   // (SubpixelSouthWest)

    // Miscellaneous const characters:
    const char CHAR_SMILY = 1;   // (Smilie)
    const char CHAR_SMILY_INV = 2;   // (SmilieInv)
    const char CHAR_HEART = 3;   // (Heart)
    const char CHAR_DIAMOND = 4;   // (Diamond)
    const char CHAR_CLUB = 5;   // (Club)
    const char CHAR_SPADE = 6;   // (Spade)
    const char CHAR_BULLET = 7;   // (Bullet)
    const char CHAR_BULLET_INV = 8;   // (BulletInv)
    const char CHAR_MALE = 11;   // (Male)
    const char CHAR_FEMALE = 12;   // (Female)
    const char CHAR_NOTE = 13;   // (Note)
    const char CHAR_NOTE_DOUBLE = 14;   // (NoteDouble)
    const char CHAR_LIGHT = 15;   // (Light)
    const char CHAR_EXCLAM_DOUBLE = 19;   // (ExclamationDouble)
    const char CHAR_PILCROW = 20;   // (Pilcrow)
    const char CHAR_SECTION = 21;   // (Section)
    const char CHAR_POUND = 156;   // (Pound)
    const char CHAR_MULTIPLICATION = 158;   // (Multiplication)
    const char CHAR_FUNCTION = 159;   // (Function)
    const char CHAR_RESERVED = 169;   // (Reserved)
    const char CHAR_HALF = 171;   // (Half)
    const char CHAR_ONE_QUARTER = 172;   // (OneQuarter)
    const char CHAR_COPYRIGHT = 184;   // (Copyright)
    const char CHAR_CENT = 189;   // (Cent)
    const char CHAR_YEN = 190;   // (Yen)
    const char CHAR_CURRENCY = 207;   // (Currency)
    const char CHAR_THREE_QUARTERS = 243;   // (ThreeQuarters)
    const char CHAR_DIVISION = 246;   // (Division)
    const char CHAR_GRADE = 248;   // (Grade)
    const char CHAR_UMLAUT = 249;   // (Umlaut)
    const char CHAR_POW1 = 251;   // (Pow1)
    const char CHAR_POW3 = 252;   // (Pow2)
    const char CHAR_POW2 = 253;   // (Pow3)
    const char CHAR_BULLET_SQUARE = 254;   // (BulletSquare)
}
